# Semantic-Segmentation-Unet
Lightweight U-Net–style model for semantic segmentation. Uses an encoder-decoder CNN with skip connections for pixel-wise classification. Supports binary and multi-class segmentation with variable input sizes. Easy to train and customize for image-mask datasets.
